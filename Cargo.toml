[package]
name = "gbnet"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.40", features = ["net", "time", "rt", "sync"] }
log = "0.4"
env_logger = "0.11"
rand = "0.8"
thiserror = "1.0"
gbnet_derive = { path = "./gbnet_derive" }

[features]
metrics = []